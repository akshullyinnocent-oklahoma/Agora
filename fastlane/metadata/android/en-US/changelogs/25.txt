* Custom font support with import/export
* web_fetch now returns full page content (it was returning only navigation)
* Image transcription morphs in place instead of flashing away
* Smoother scale+fade animations for thought blocks and appended steps
* Tap anywhere on the embedding provider/model fields to open the picker
* Custom OpenAI providers auto-resolve the /v1 Base URL
* Generation parameter reset is now instant
* Edit / delete / regenerate are disabled while a response is generating
* Consistent bottom spacing across all settings pages
* Fixes: message-delete race (no more zombie messages), out-of-memory on large export, double scroll on a new chat's first message, rating dialog double title
